version: '3.8'


services:
  nextjs:
    container_name: prothomalo_scrapping_frontend
    build:
      context: .
    restart: unless-stopped
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
